export type ViewReference = {
    viewColumn: string;
    source: {
        schema: string;
        table: string;
        column: string;
    } | undefined;
};
declare function parseViewDefinition(selectStatement: string, defaultSchema: string): ViewReference[];
export default parseViewDefinition;
